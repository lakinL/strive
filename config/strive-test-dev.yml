
server:
  port: 8763

spring:
  application:
    name: test

management:
   endpoints: 
     web:
       exposure: 
         include: hystrix.stream 

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
